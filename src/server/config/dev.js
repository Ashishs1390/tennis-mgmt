const config = {
    dbUrl: `${process.env.MONGO_URI}`
}

module.exports = config;
// || `${process.env.MONGO_URI}`

// mongodb + srv://ashishs104:Arsenal@1886@cluster0.tr5qs.mongodb.net/?retryWrites=true&w=majority